import 'package:flutter/material.dart';

class SuitorProfileScreen extends StatelessWidget{
  const SuitorProfileScreen({super.key});

  @override
  Widget build(BuildContext context){
    return Scaffold(
      appBar: AppBar(title: Text("Suitor Profile Screen")),
      body: Center(child: Text("Suitor Profile Screen")),
    );
  }
}